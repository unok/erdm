{{define "dot_tables"}}
{{range $tk, $t := .Tables}}
  {{.TitleReal}} [label = <<table border="0" cellborder="0" cellpadding="0">
    <tr><td colspan="2"><font face="Ricty-Bold">
    {{- .Title}}/{{.TitleReal -}}
    </font></td></tr>
    {{- range $k, $c := .Columns}}
    <tr><td align="left">
    {{- .Title}}/{{.TitleReal}}  {{.Type}} {{if (or (not .AllowNull) .IsUnique) -}}
    (UNN)
    {{- else if not .AllowNull -}}
    (NN)
    {{- else if .IsUnique -}}
    (U)
    {{- end}}</td><td>{{if .IsPrimaryKey -}}
    P
    {{- end}}{{if .IsForeignKey -}}
    F
    {{- end}}</td><td>
    {{- range $ik, $iv := .IndexIndexes}}I{{$iv}} {{end}}</td></tr>{{end}}</table>>];
{{end}}{{end}}